"""The Skein Lite MCP (Model Context Protocol) server package.

Named mcp_server/ rather than mcp/ on purpose: the official SDK imports as `mcp`, so a top-level
mcp/ package would shadow it. See mcp_server/server.py for the tool surface.
"""
