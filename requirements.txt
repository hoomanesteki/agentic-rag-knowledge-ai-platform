# Runtime dependencies. Kept minimal on purpose.
# Heavier libraries are added at the milestone that first needs them:
#   M1: qdrant-client, groq, voyageai   (models are hosted, nothing runs locally)
#   M3: fastapi, uvicorn, python-jose, passlib
#   M4: duckdb, dbt-duckdb
#   M5: neo4j
#   M6: langgraph, langchain
#   M8: mlflow, ragas
python-dotenv~=1.0
pyyaml~=6.0
