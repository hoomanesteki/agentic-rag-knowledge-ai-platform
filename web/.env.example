# Copy to web/.env.local for local dev. Next.js only reads env files from this directory.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Set to enable the Turnstile captcha on login (leave unset in dev to hide it).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
